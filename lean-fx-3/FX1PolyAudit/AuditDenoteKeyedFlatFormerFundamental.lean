import FX1PolyAudit.DependencyAudit
import FX1Poly.Typed.Metatheory.Denote.Universe.DenoteKeyedFlatFormerFundamental

/-! # FX1PolyAudit/AuditDenoteKeyedFlatFormerFundamental
    — zero-axiom gate for the route-A-FREE flat-former universe-membership FT arm (FTGEN-5, incl. equivCode)

A flat former (arrow / product / sum / either / equivCode) is a denote-reducible MEMBER of its classifying
universe given only its strong normalization — the denote-reducible-TYPE obligation discharged unconditionally
via `IsReducibleTypeAtAllDenoteLevels.ofDataFlat` (no route-A composite-domain piArm).  `equivCodeFundamental
AtDenote` is the headline instance, closing the equivCode formation-FT coverage the FTGEN-2 census deferred.
Must be free of `propext`, `Quot.sound`, `Classical.choice`, `sorry`, `native_decide`, `omega`. -/

namespace FX1PolyAudit

#assert_no_axioms FX1Poly.Typed.flatFormerFundamentalAtDenote
#assert_no_axioms FX1Poly.Typed.equivCodeFundamentalAtDenote

end FX1PolyAudit
