import FX1PolyAudit.Corpus.SelfVerifiedMetatheory

/-! # FX1PolyAudit/SelfVerifiedMetatheory — thin re-export shim pending removal.

This module's content moved to `FX1PolyAudit.Corpus.SelfVerifiedMetatheory` during the audit-directory
restructure (cross-cutting corpus/FX0/ledger cluster out of the root).
This shim re-exports it so importers naming `import FX1PolyAudit.SelfVerifiedMetatheory`
keep resolving until the orchestrator repoints them and removes the shim. -/
