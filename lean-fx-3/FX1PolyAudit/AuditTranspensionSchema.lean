import FX1PolyAudit.DependencyAudit
import FX1Poly.Typed.Dimensions.Parametricity.TranspensionSchema

/-! # FX1PolyAudit/AuditTranspensionSchema — zero-axiom gate for the transpension row-schema

Per-declaration zero-axiom gate for `FX1Poly/Typed/Dimensions/Parametricity/TranspensionSchema.lean`: the
parametric row-schema (`transpensionInstanceAt` / `instanceToIotaRow` / `gelInstanceData`), the internal
derivation (`gelBetaIotaRow_isGeneratedBySchemaAtAffine` — the shipped gel-β row is generated by the schema
at affine) and its operational link (`schemaAffineRowComputes`), the schema-at-classification tie
(`schemaAtGelStructureClass`), non-vacuity (`transpensionSchema_discriminates`), and the honest liveness
ledger (`transpensionInstanceIsLive` / `liveStructureClassCount` / the markers).

Every declaration below must be free of `propext`, `Quot.sound`, `Classical.choice`, `sorry`,
`native_decide`, `omega`. -/

namespace FX1PolyAudit

#assert_no_axioms FX1Poly.Typed.instanceToIotaRow
#assert_no_axioms FX1Poly.Typed.gelInstanceData
#assert_no_axioms FX1Poly.Typed.transpensionInstanceAt
#assert_no_axioms FX1Poly.Typed.gelBetaIotaRow_isGeneratedBySchemaAtAffine
#assert_no_axioms FX1Poly.Typed.schemaAtGelStructureClass
#assert_no_axioms FX1Poly.Typed.schemaAffineRowComputes
#assert_no_axioms FX1Poly.Typed.transpensionSchema_discriminates
#assert_no_axioms FX1Poly.Typed.transpensionInstanceIsLive
#assert_no_axioms FX1Poly.Typed.affineInstanceIsLive
#assert_no_axioms FX1Poly.Typed.cartesianInstanceIsReserved
#assert_no_axioms FX1Poly.Typed.liveStructureClassCount

end FX1PolyAudit
