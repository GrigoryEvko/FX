import FX1PolyAudit.Corpus.LambdaValueFuzzFamily

/-! # FX1PolyAudit/LambdaValueFuzzFamily — thin re-export shim pending removal.

This module's content moved to `FX1PolyAudit.Corpus.LambdaValueFuzzFamily` during the audit-directory
restructure (cross-cutting corpus/FX0/ledger cluster out of the root).
This shim re-exports it so importers naming `import FX1PolyAudit.LambdaValueFuzzFamily`
keep resolving until the orchestrator repoints them and removes the shim. -/
