import FX1PolyAudit.DependencyAudit
import FX1Poly.Polygraph.TwoCategory.WalkingAdjunction.ArcCupScanAssembly

/-! # FX1PolyAudit/Polygraph/TwoCategory/WalkingAdjunction/ArcCupScanAssembly — zero-axiom gate

Per-declaration zero-axiom gate for the cup-head partner-scan assembly (peel campaign H,
cup rung 2b): the puncture shape of the shift-image candidate range and the map-congruence
assembly riding the composite `partnerIndexOf` on the joined-fresh punctured scan.

Every declaration must be free of `propext`, `Quot.sound`, `Classical.choice`, `sorry`,
`native_decide`, `omega`. -/

namespace FX1PolyAudit

#assert_no_axioms FX1Poly.Polygraph.rangeMapShift_splitsAtWindow
#assert_no_axioms FX1Poly.Polygraph.arcCupHeadFolded_partnerScanCorr

end FX1PolyAudit
