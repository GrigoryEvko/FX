import FX1PolyAudit.DependencyAudit
import FX1Poly.Typed.Metatheory.Denote.Universe.DenoteKeyedFlatFormerFundamental

/-! # FX1PolyAudit/AuditDenoteKeyedFlatFormerFundamental
    — zero-axiom gate for the route-A-FREE CONTENT-FREE flat-former universe-membership FT arm (FTGEN-5)

A content-free flat former (`isFlatDataCode = true` AND `carrierCombinator? = none` — arrow / sum) is a
denote-reducible MEMBER of its classifying universe given only its strong normalization — the
denote-reducible-TYPE obligation discharged unconditionally via `IsReducibleTypeAtAllDenoteLevels.ofDataFlat`
(no route-A composite-domain piArm).  The carrier-aware formers (product / either / equiv) route carrier-aware
via `…FundamentalFromCarriers`; equivCode's content-free instance was subsumed by `equivCodeFundamentalFrom
Carriers` at FTGEN-5.5.  Must be free of `propext`, `Quot.sound`, `Classical.choice`, `sorry`, `native_decide`,
`omega`. -/

namespace FX1PolyAudit

#assert_no_axioms FX1Poly.Typed.flatFormerFundamentalAtDenote

end FX1PolyAudit
