import LeanFX2.Algo.Progress.Headline.FinalProgress

/-! # LeanFX2.Algo.Progress.Headline

Compatibility umbrella for the Wright-Felleisen progress headline.
The implementation is split into semantic leaves under
`LeanFX2.Algo.Progress.Headline.*` so constructor-family edits rebuild
only their focused proof module before the final assembly theorem.
-/
