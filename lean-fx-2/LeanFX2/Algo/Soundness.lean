import LeanFX2.Algo.Soundness.EvalSound

/-! # Algo/Soundness

Compatibility umbrella for algorithmic soundness. Import the semantic
leaves under `LeanFX2.Algo.Soundness.*` when a consumer only needs
base iota cases, payload iota cases, `headStep?` closure, or the
end-to-end evaluator theorem.
-/
