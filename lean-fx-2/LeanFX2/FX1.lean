prelude
import LeanFX2.FX1.Core

/-! # LeanFX2/FX1

Separate umbrella for the minimal FX1 root calculus.

FX1 is the future small trusted spine for Lean-in-FX bootstrapping.  It stays
outside the rich `LeanFX2` production umbrella so dependency audits can keep the
minimal root and the current feature-rich engine as distinct import surfaces.
-/

namespace LeanFX2.FX1

end LeanFX2.FX1
